<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
        <div class="comment-content" id ="this_comment_<%= comment.id %>"></div>
        <p><%= comment.content %></p>
        <p><%= link_to "Edit", edit_publication_comment_path(publication, comment), remote: true %>&nbsp;&nbsp;&nbsp;&nbsp;
        <%= link_to "Delete", publication_comment_path(comment.publication_id, comment.id), method: :delete, remote: true,
                                        data: { confirm: "Do you really want to delete this?"} %></p> <hr>
    <% end %>
  <% end %>
</ul>